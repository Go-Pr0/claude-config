# Dependency versions

Do not bump, pin, downgrade, or otherwise change package, runtime, or tooling versions (npm/yarn/pnpm deps, Node, Tailwind, compilers, lockfile churn for upgrades, etc.) unless the user explicitly asks. Prefer the versions already in the repo. If a version change is unavoidable to complete requested work, keep it minimal and call it out.
