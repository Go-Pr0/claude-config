
## Scope

- Do only what was asked.

## Shell

- Prefer `rg`, `rg --files`, `Get-Content -Raw`, `Select-String`, and `-LiteralPath`.
- Bash commands run through the `rtk` token-compression proxy via a PreToolUse hook. See `@RTK.md` for its quirks and correct invocations.
